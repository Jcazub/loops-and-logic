for ( var x = 1; x <= 10; x++){ //in for loops, we need to initialize a variable, create a condition in which the loop will end, and then do soemthing with te variable each time the loop runs
  console.log(x);
}

var y = 7;
while (y > 0){
  console.log(y)
  y--; //The conditional must at some point stop, so there must be something that happens to the variable each time.
}
